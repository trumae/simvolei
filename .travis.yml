language: cpp
dist: trusty
compiler:
- gcc 

before_script:
  - mkdir build
  - cd build

matrix:
- os: linux
    env:
      - TEST="BuildTest"
    addons:
      apt:
        packages:
          - gcc
          - g++
    script:
      - cmake ..
      - make
      - make check
